# Полный статус проекта Launcher — handoff 2026-09-20

Документ для переноса работы в другой чат. Состояние на 20.09.2026, ~08:00 MSK.

## 1. Что такое Launcher

F-Engineering Launcher v3 — локальное Windows-приложение (без облака) для инженеров:
открывает папку объекта → дерево файлов (окно 1) → лента миниатюр (окно 2) →
большой просмотр (окно 3). Форматы: PDF, Excel (XLS/XLSX/XLSM/CSV через HTML),
Word (через PDF), картинки (JPG/PNG/TIF/GIF…), TXT, DWG (через PDF-пару или
рендер AutoCAD COM), остальное — нативные карточки. Бэкенд — Python HTTP
(`app/backend/server.py`, порт `8780`), фронт — чистый JS (`app/frontend/app.js`,
~4500 строк), без сборщиков и Node-зависимостей (`requirements.txt`: openpyxl,
pymupdf, pypdf, pywebview).

## 2. Пути

- GitHub: `f-engineering-spb/f-engineering-launcher`, ветка **`dvg-main`**
  (единственная; тегов и других веток на remote нет; default = `dvg-main`).
- Рабочая папка (актуальная): `C:\Users\a9379\Documents\Codex\FEngineering_Launcher_v3_CURRENT_COPY`
- Старая папка (НЕ использовать, история до чистки): `...\FEngineering_Launcher_v3_OLD_DO_NOT_USE`
  (успешно переименована, процессы остановлены).

- Внешний бэкап (не база, только на чтение):
  `C:\Users\a9379\Documents\Codex\_BACKUP\FEngineering_Launcher_v3_2026-09-18`
- Данные объектов: диск `H:` (Google Drive). 19.09 был недоступен, 20.09 снова
  доступен. Тестовые объекты: AFI Development, Legenda (05_Legenda_Leningradskoe).

## 3. История dvg-main (6 коммитов, корень 741ebdd)

1. `741ebdd` — чистая база (80 файлов).
2. `0ecde4f`, `7bdd558` — доки (dvg-main единственная база, запрет архивов).
3. `c61c956` — выделение: одиночный/Shift/Ctrl в дереве И в ленте, сброс по
   пустому месту, якорь после фильтра, подсветка карточек `.selected`.
4. `4c88228` — DWG очередь: семафор 1 (CAD-COM), PDF параллельно 3.
5. `a11bf72` — прогресс пакетного DWG («Модель N из M», честный финиш).
- НЕ закоммичено (рабочее дерево): чистка ZWCAD в 11 md + центрирование окна
  AutoCAD в `server.py` (см. п.6).

## 4. Что решено (кроме DWG)

- Лента Excel в AFI: сервер читает все 4 книги; стрелки/колесо/синхронизация
  работают через зоны (`activeNavZone`); iframe карточек `pointer-events:none`.
- Честный прогресс (не виснет на 92%), отмена рендера гасит эпоху и слоты.
- Чистки кода (два раунда, −1400 строк): мёртвые функции, `.bak`, дебаг-принты,
  дубликаты; `node --check` / `py_compile` чистые.
- Оценка одинаковых имён в разных папках: сравнение всегда по полному пути.
- ZWCAD: в коде его не было (вырезан раньше, коммит `48a0783`); из 11 md-доков
  упоминания удалены (не закоммичено).

## 5. Стадия DWG: что сделано и что нет

- Аудит: пары строит только фронт (`findPdfPairForDwg`, глобально по имени +
  `mirrorScore` хвоста папок); контейнеры PDF/DWG код не знает; нормализация
  нечувствительна к регистру/пробелам/ё; склейки по одному номеру нет
  (доказано выполнением); неоднозначность молча берёт первого — не исправлено.
- Сделано: очередь (задача 1), прогресс (задача 2) — обе проверены симуляцией
  настоящего кода, живьём через CAD не гонялись.
- НЕ сделано: явная ошибка ненастроенного AutoCAD (задача 3), ZWCAD (не нужен),
  контейнерное правило пар, статус неоднозначности, одиночный-DWG прогресс
  сверх существующего.
- Заглушки вместо DWG-картинок: первопричина — недоступный `H:` (400 от сервера
  → `missing-preview`) и медленный CAD (демон стартует ~50 с, рендер 11 МБ дольше
  280 с; успешные рендеры от 13.08 лежат в `runtime/dwg-probe`). Код цепочки цел.

## 6. Последняя проблематика: окно AutoCAD слева вверху + мигание

- Пользователь открывает DWG через **ассоциацию ОС** (`mode: system-default`),
  где окно ищет 120-секундный сторож по имени файла без центрирования и долбит
  фокус каждые 2 с — отсюда мигание; первый патч стоял на пути custom-exe,
  которым пользователь не пользуется, плюс запускалась старая папка без патча.
- Внесено (не закоммичено): общий `_center_window_on_screen` (центр без смены
  размера/фокуса, мимо развёрнутых) + центрирование в обоих сторожах + стоп
  после 6 неудач фокуса (~12 с вместо 120 с мигания). Диагностика пишется в
  `runtime/logs/native-front.log` (`centered-before/after`).
- Для проверки: закрыть старый сервер, запустить
  `..._CURRENT_COPY\scripts\start_windows.cmd`, открыть DWG, смотреть окно и
  хвост `native-front.log`. В момент записи пользователь ещё на старой папке.

## 7. Открытые пункты

1. Закоммичено и отправлено в `dvg-main`: ZWCAD-доки, центрирование окна AutoCAD (`7d1d942`), ошибка ненастроенного AutoCAD (`c94d8c3`), относительные пути в скриптах (`42e78e1`), управление процессами сервера (`2feffe0`), документация CLI-агента (`docs/AGENTS.md`, `AGENTS.md`) и хелпер `RUN_AGENT.cmd`.
2. Переименование папки: старая заблокированная папка `FEngineering_Launcher_v3` успешно освобождена от зависших процессов и переименована в `FEngineering_Launcher_v3_OLD_DO_NOT_USE`.
3. Контейнерные пары, статус неоднозначности — по отдельным командам.
4. Живые проверки мышью всего DWG-пути (у агента мыши нет).


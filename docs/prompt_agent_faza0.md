# Задание для агента: восстановление скорости + точечные исправления (фаза 0)

## Контекст и стартовая точка

- Локальный репозиторий: `C:\Users\a9379\Documents\Codex\FEngineering_Launcher_v3`
- GitHub: `f-engineering-spb/f-engineering-launcher`, рабочая ветка: `review/v3.3-updates`, база сравнения: `review/v3.2-base`
- Прочитать первым: `docs/DWG_ROADMAP_NEXT.md` (ветка `review/v3.3-updates`) — там состояние проекта, три ситуации DWG и ловушки
- Зафиксированный контракт кликов PDF/Word/Excel/картинок: тег `checkpoint/2026-09-14-pdf-ok` — **НЕ ЛОМАТЬ**
- Заказчик получил внешнее ревью кода. Ниже — факты из этого ревью. Их не нужно переспоривать, они проверены по коду коммитов `4b3b856`, `2295ba8`, `6a0e1ee`.

## Установленные факты (принять как данность)

1. Сервер `app/backend/server.py` — `ThreadingHTTPServer`, порт 8780. Сервер **многопоточный**. Гипотеза «однопоточность блокирует всё» — неверна.
2. Индекс пар PDF/DWG строится один раз при открытии объекта (`openSelectedObject` → `state.pdfPairIndex`), ~0,3 с на 14 тыс. файлов (замер на 03_ЖК МОД: 476 мс). **Индекс пар НЕ является причиной медленной загрузки дерева.**
3. Найден баг: `previewFileDirectly` в `app/frontend/app.js` (двойной клик по файлу в дереве) на **каждый** вызов выполняет `flattenTree` всего манифеста + `buildPdfPairIndex(allNodes)` заново, вместо использования уже построенного `state.pdfPairIndex`.
4. Узкие места DWG (сейчас НЕ трогать, только помнить):
   - старт демона: до 240 с ожидания под блокировкой `_DWG_DAEMON_LOCK` — параллельные DWG-запросы выстраиваются в очередь за ним;
   - таймаут конвертации одного файла: 600 с (`DWG_RENDER_TIMEOUT_SECONDS`);
   - если демон не поднялся — fallback `dwg_convert_oneshot` поднимает новый AutoCAD на каждый файл (30–90 с на файл).
5. В журналах фиксировался случай параллельно работающего старого окна со старым JS (призрачные запросы `firstPageOnly:true` пачками). Отсюда правило гигиены ниже.
6. Диагностика уже встроена в код: `append_render_debug` → `runtime/logs/render-debug.jsonl`, эндпоинт `/api/client-log`. Использовать её, не изобретать новую.

## Цель фазы 0

Восстановить быструю загрузку объекта и показ файлов так, чтобы скорость не зависела от DWG-механизмов. Архитектурное правило: **ничего DWG-специфичного не имеет права замедлять или блокировать базовые потоки (дерево, лента, показ PDF/Word/Excel/картинок)**.

Метод: сначала измерить, потом чинить точечно. Никаких переписываний конвейера показа.

## Задачи по порядку

### 1. Гигиена тестирования (обязательно, до всего остального)

- Закрыть ВСЕ окна лаунчера перед началом работ. Работать в одном окне.
- После каждой правки фронта — перезагружать окно (F5) и проверять версию сборки в заголовке окна. Сервер отдаёт файл с диска, рестарт сервера для фронта не нужен.
- Тестовые DWG копировать локально: `C:\Users\a9379\AppData\Local\Temp\opencode\daemon_test\`. Не читать тестовые файлы напрямую с живого облака H: (папки могут исчезать посреди теста).
- НЕ запускать тяжёлые CAD-конвертации без явного разрешения пользователя.

### 2. Инструментация (главное в этой фазе)

Добавить через существующий `append_render_debug` замеры этапов с длительностью в мс:

- `object-open`: время GET манифеста. Обязательно различать: чтение готового манифеста из `runtime/manifests/<id>.json` vs полный скан папки по сети.
- `pair-index`: время `buildPdfPairIndex`.
- `tree-render`: время `renderTree` (от начала до вставки последнего узла в DOM).
- `display-batch`: для каждого `renderBatch` — количество файлов, длительность, доля cacheHit.

Снять замеры на двух объектах: большой (ЖК МОД / 03_ЖК МОД, 14–15 тыс. файлов) и маленький (~100 файлов). Для каждой цифры записать: чтение из кэша или скан.

### 3. Точечные исправления (маленькие правки, без переписываний)

a) **`previewFileDirectly`**: использовать `state.pdfPairIndex`; если он null — построить один раз и сохранить в state. Не пересобирать на каждый клик.

b) **Флаг `pairless`** (первый пункт ситуации 2 из роадмапа): если в манифесте PDF = 0 (или доля точных пар в первых ~50 DWG нулевая) — индекс пар не строить вовсе, все DWG сразу идут как `DWG_MODEL`, без поиска пар. Проверка должна быть дешёвой и выполняться при открытии объекта.

c) **Фоновая проверка изменений** (`startDiffPolling` → `refreshObjectInPlace`): на время диагностики увеличить интервал до ≥ 10 минут или отключить по флагу. Проверить по логам, не запускает ли она полный скан H: при открытии объекта или во время показа.

d) **Дерево**: если замер `tree-render` на 15 тыс. файлов > 2 с — сделать ленивое раскрытие узлов (не рендерить все папки сразу, только видимую верхнюю часть, остальное — при раскрытии). По аналогии с уже работающей пагинацией ленты (50 файлов + «Загрузить ещё»).

### 4. Что НЕ делать (критично)

- Не трогать: контракт кликов (тег `checkpoint/2026-09-14-pdf-ok`), пагинацию ленты (50 + load more), протокол демона (job/done-файлы), `open_in_explorer`, UTF-8 цепочку выбора папки, кэш `runtime/cache`.
- Не переписывать конвейер показа заново: в истории уже был откат `2895aff` именно из-за этого — «оптимизации» сломали выделение и показ.
- PowerShell-скрипты сохранять только с UTF-8 BOM; `server.py` править с CRLF (байтовые патчеры с `\r\n`); висячих запятых в PS-массивах не оставлять.

## Протокол приёмки

1. Открытие объекта ЖК МОД из кэша манифеста: ≤ 3 с до полностью отрисованного дерева.
2. Показ 100 PDF: ≤ 15 с до последней миниатюры в ленте.
3. Одиночный DOC на 35 страниц: все 35 страниц в ленте, клик по 10-й показывает 10-ю, стрелки идут внутри документа.
4. Двойной клик по файлу в дереве на большом объекте: отклик < 1 с (в логах видно, что `buildPdfPairIndex` не вызывается повторно).
5. Повторное открытие того же объекта: в логах нет полного перескана H:.
6. Папка без PDF вообще (`pairless`): открытие и показ не медленнее, чем обычной папки того же размера.

## Форма отчёта (обязательно)

Прислать по завершении:

1. Таблица «до/после»: все замеры из раздела 2 в мс, по обоим объектам.
2. Список изменённых файлов с кратким описанием каждой правки (одна строка на файл).
3. Что не получилось и почему (если такое есть).
4. Фрагмент `runtime/logs/render-debug.jsonl` за финальный тестовый прогон.
5. Собственное мнение: что сделал бы иначе и какие следующие шаги видишь (один абзац).

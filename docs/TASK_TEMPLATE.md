# Шаблон задания для агента (F-Engineering Launcher)

Скопируй этот файл в новое задание и заполни все поля. Задание хранится
в библиотеке `E:`, выполняется в локальной копии репозитория, код
возвращается только через GitHub.

## 1. Цель

> Одним-двумя предложениями: что должно измениться для пользователя.

## 2. Контекст и источники (прочитать первыми)

- [ ] Ветка: `review/v3.3-updates`, базовый коммит: `________`
- [ ] Документы: `________________`
- [ ] Связанные отчёты из `E:\...\09_Launcher`: `________________`

## 3. Что сделать

1. ...
2. ...

## 4. Что НЕ делать (запреты по умолчанию)

- [ ] Не трогать живые данные `H:` / `E:` без явного пункта в разделе 3.
- [ ] Не запускать тяжёлый AutoCAD / DWG-конвертацию без отдельного разрешения.
- [ ] Не использовать порт 8000; проверять только на `http://127.0.0.1:8780/`.
- [ ] Не создавать health-check, cron/schedule, auto-sync, фоновые мониторинги.
- [ ] Не делать Git-репозиторий внутри `E:`, не копировать код вручную
      между `E:` и локальной копией.

## 5. Проверка (обязательна)

- [ ] Дерево объекта открывается.
- [ ] PDF: ___ стр. — превью есть / нет.
- [ ] DOC/DOCX многостраничный: ___ стр. — превью есть / нет.
- [ ] Excel: листы ___ — превью есть / нет.
- [ ] Картинка: ___ — превью есть / нет.
- [ ] Прогресс у многостраничного документа: показан / не показан.
- [ ] DWG/тяжёлый CAD: не запускался / запускался (разрешение: ___).

## 6. Результат

- Отдельный коммит(ы) в `review/v3.3-updates`, пуш в GitHub.
- Финальный отчёт — строго по `docs/REPORT_TEMPLATE.md`, одним сообщением,
  без промежуточных вопросов. Всё, что можно проверить самому
  (git, файлы, процессы, консоль), пользователь делать не должен.

# Задача: починить листание страниц многостраничных документов (лента миниатюр + стрелки)

> Дата: 13 сентября 2026. Статус: НЕ РЕШЕНО, второй час работ. Передаётся следующему интеллекту.

## 1. Окружение и пути

- Репозиторий: `C:\Users\a9379\Documents\Codex\FEngineering_Launcher_v3`, ветка `main`.
- Сервер: `app\backend\server.py`, `http://127.0.0.1:8780/`.
- Фронт: `app\frontend\app.js` (~3900 строк), `index.html`, `styles.css`.
- Эталонная рабочая версия: `origin/main`, коммит `cf13cbe52874955c28bdb819d6e1349a7f5e8417`.
- Локально поверх неё: 17 коммитов + незакоммиченные правки в `app.js`, `index.html`, `styles.css`, `server.py`, `flauncher.pyw`.
- Тестовый файл: `H:\Общие диски\007_Коммерческий отдел\ОБЪЕКТЫ 2026\05_Legenda_Leningradskoe_street\09_Входящие_2_круг\2_круг_тендер\Договор\Договор выше 0_МСК_БЕЗ СУБПОДРЯД_ИЗМ 28.07.2026.docx` (35 страниц).
- Журналы: `runtime\logs\render-debug.jsonl` (запросы рендера), клиентских ошибок нет (ловушка `installClientErrorTrap` в начале `app.js`, лог должен был появиться как `client-errors.jsonl` — его нет).

## 2. Симптомы со слов пользователя (воспроизводятся в окне лаунчера)

1. Выделить один многостраничный файл → «Отобразить»: в ленте только 1-я страница, остальные 34 не появляются.
2. Выделить все DOC → «Отобразить»: все страницы всех документов в ленте есть, мышью крутятся. Но клик по любой странице N>1 «превращает» её в 1-ю — дальше листать нельзя.
3. Стрелки ←/→: вместо перехода по страницам внутри документа метка в дереве перепрыгивает на следующий файл.
4. Требование: клик по файлу — все его страницы в ленте; стрелки идут внутри документа, метка дерева держит файл до последнего листа; клик по миниатюре — крупный показ именно этой страницы; возврат из полноэкранного — в ту же позицию.

## 3. Проверенные факты (не гадать заново)

- Сервер отдаёт всё корректно: `POST /api/word/render {firstPageOnly:false}` → 35/35 за ~14 с, ошибок нет. `/api/pdf/page` для страниц 2..35 — все 200 OK (видно в `render-debug.jsonl`: сотни успешных `pdf/page`, десятки `word/render`).
- Логика сопоставления и дедупликации проверена в Node на живом ответе сервера: 35 элементов → 35 уникальных ключей `documentPath|page`, дубликатов 0.
- Автотест в обычном браузере (`http://127.0.0.1:8780/?autotest=wordflow`, код в конце `app.js`, функция `runWordFlowAutotest`): после рендера 35 миниатюр и 35 записей в состоянии, показ работает, 40+ секунд стабильно, ничего не стирается.
- Исключений в браузере нет (ловушка молчит).
- Вывод: данные и конвейер целы. Ломается где-то между готовыми данными и тем, что видит пользователь в ОКНЕ ЛАУНЧЕРА (pywebview). Подозрение №1 — в окне крутится старый JS (см. п.5).

## 4. Что сравнить с эталоном `origin/main` (обязательно)

В эталоне НЕТ: `firstPageOnly`, `streamRemainingDocumentPages`, `appendPagesToViewer`, `activeRenderSig`, `collectCompleteLoadedPages`, `streamedDocKeys`. Вместо этого:
- `renderBatch` запрашивает каждый файл ЦЕЛИКОМ, без обрезки страниц;
- лента перестраивается целиком из полных групп (`renderPdfViewer(pageGroups.flat())` при каждом прогрессе);
- стрелки идут по полному `state.renderedPages`, показ — `showPdfPage(nextPage)`, метка дерева — через `revealPathInTree`.

Координаты текущего кода для сверки: `renderSelectedPdfFiles` (~строка 2839), вложенный `renderBatch` (~2961), `showPdfPage` (2376), `appendPagesToViewer` (2119), `createPageThumbElement` (2053), обработчик стрелок (~3663), `collectPreviewFilesForDisplay` (1751), `loadMorePdfFiles` (~3137, второй `renderBatch` ~3156), автотест (3844).

## 5. Проверенные подозреваемые (по приоритету)

1. **Старое окно/старый JS.** В журнале есть запросы `firstPageOnly:true` ПАЧКАМИ ПО 8 ВПЛОТЬ ДО ПОСЛЕДНЕЙ МИНУТЫ — такой код в текущей сборке остался только в одном месте, а раньше был везде. Значит, параллельно работает окно со старым кодом. Перед отладкой: закрыть ВСЕ окна и вкладки лаунчера, открыть заново, проверить заголовок окна (туда выводится версия сборки, код добавлен) и повторить. Если симптомы ушли — это было оно.
2. **Сцена не следует за лентой.** После полного рендера при уже установленном `activePageKey` новый показ страницы не вызывается — сцена показывает старый титульник, хотя лента полная. Кандидат №1 на «вижу только первую».
3. **Клик по миниатюре N показывает страницу 1.** Возможные места: замыкание обработчика на объекте страницы, несвежий `activePageKey`, повторный показ с `pages[0]`. Идти от обработчика клика (`createPageThumbElement`, ~2100) вниз по `showPdfPage` с живым дебагом.
4. **Гонки повторных рендеров** (клик-клик-Отобразить): каждый стирает ленту (`replaceChildren`) и стартует заново. Частично прикрыто флагом `activeRenderSig` (~2852) — проверить его сброс на всех путях выхода.
5. **Фоновая проверка изменений** (`startDiffPolling` → `refreshObjectInPlace` → `renderSelectedFiles`): на Google Drive может дёргать перерендер папки и сносить полные наборы обратно в титульники. Проверить частоту в журнале.

## 6. Ограничения (не сломать)

- Большие папки (ЖК МОД, 14 тыс. файлов) обязаны открываться: постраничная подгрузка СПИСКА файлов (`FIRST_PAGE_SIZE`/`loadMore`) — сохранять.
- Не трогать: открытие в проводнике (`open_in_explorer`, раздельные аргументы `/select,`), UTF-8 цепочку выбора папки, DWG-рендер, Excel-просмотр.
- Временная диагностика (лог `render-debug.jsonl`, ловушка ошибок, `?autotest=wordflow`, `runWordFlowAutotest`) — удалить только ПОСЛЕ решения, не раньше.

## 7. Протокол приёмки

1. Один файл-договор → «Отобразить» → в ленте 35 миниатюр.
2. Клик по 10-й → на сцене 10-я (счётчик «Стр. 10 из 35»).
3. Стрелки от 1-й до 35-й идут внутри договора, метка дерева стоит на договоре.
4. На 35-й стрелка вправо → следующий файл, метка перепрыгивает.
5. Двойной щелчок → весь экран; Esc → возврат в ту же позицию.
6. То же для PDF-документа и для выбора всех DOC папки.

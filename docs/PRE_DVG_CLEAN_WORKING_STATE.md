# PRE_DVG_CLEAN_WORKING_STATE — superseded

This checkpoint no longer exists. The project now has a single baseline:

- branch `dvg-main`, current HEAD only;
- no tags, no old branches, no old commits.

Do not use any previous checkpoint, tag, branch, or external copy
(including `_BACKUP`) as a source of code. Work only from the current
`dvg-main` checkout.
